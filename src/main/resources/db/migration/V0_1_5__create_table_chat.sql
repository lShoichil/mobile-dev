CREATE TABLE chat
(
    id      BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    name    VARCHAR(255),
    type_id BIGINT,
    CONSTRAINT pk_chat PRIMARY KEY (id)
);

ALTER TABLE chat
    ADD CONSTRAINT FK_CHAT_ON_TYPE FOREIGN KEY (type_id) REFERENCES chat_type (id);